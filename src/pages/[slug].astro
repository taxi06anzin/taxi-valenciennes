---
import Layout from '../layouts/Layout.astro';
import cities from '../data/cities.json';
import { CheckCircle } from 'lucide-react';

export async function getStaticPaths() {
  return cities.map(city => ({
    params: { slug: city.slug },
    props: { city },
  }));
}

const { city } = Astro.props;
---

<Layout 
  title={city.metaTitle || `Taxi à ${city.name} | Conventionné CPAM & VSL`} 
  description={city.metaDesc || `Réservez votre taxi à ${city.name}. Transport médical VSL conventionné CPAM. Lun-Ven 8h-18h. Nuit/Week-end sur réservation 24h avant.`}
>
  <!-- City Specific Hero -->
  <section class="relative bg-primary text-white py-20">
    <div class="container mx-auto px-4 text-center">
      <h1 class="text-4xl md:text-5xl font-bold font-heading mb-6">
        Taxi à <span class="text-accent">{city.name}</span>
      </h1>
      <p class="text-xl text-slate-300 max-w-2xl mx-auto mb-8">
        Votre chauffeur de taxi VSL conventionné intervient rapidement sur <strong>{city.name}</strong> et ses environs pour tous vos trajets médicaux ou privés.
      </p>
      <div class="flex justify-center gap-4">
        <a href="tel:0663039516" class="bg-accent hover:bg-amber-600 text-white font-bold py-3 px-8 rounded-full shadow-lg transition-transform hover:scale-105">
          📞 Appeler le Taxi
        </a>
      </div>
    </div>
  </section>

  <!-- Local Service Content -->
  <section class="py-16">
    <div class="container mx-auto px-4 max-w-4xl">
      <div class="prose prose-lg mx-auto text-slate-600">
        <h2 class="text-primary font-heading">Service de Taxi VSL à {city.name}</h2>
        <p>
          Besoin d'un taxi au départ ou à l'arrivée de <strong>{city.name}</strong> ? Notre société de taxi valenciennoise assure une desserte quotidienne de votre commune.
          Que ce soit pour une consultation à l'hôpital, un train à prendre ou une course urgente, nous sommes à votre disposition.
        </p>

        <div class="bg-blue-50 border-l-4 border-blue-500 p-6 my-8 rounded-r-lg">
          <h3 class="text-blue-900 font-bold text-lg m-0 mb-2">Transport Médical Conventionné</h3>
          <p class="m-0 text-blue-800">
            Nous sommes agréés CPAM pour le transport de malades assis. Si vous résidez à {city.name} et avez une prescription médicale de transport (Bon de transport), vous pouvez bénéficier du tiers payant (100% ou 65%).
          </p>
        </div>

        <h3>Pourquoi choisir notre taxi pour vos trajets depuis {city.name} ?</h3>
        <ul class="space-y-2 list-none pl-0">
          <li class="flex items-center gap-2"><CheckCircle className="w-5 h-5 text-medical flex-shrink-0" /> <strong>Connaissance parfaite</strong> des rues de {city.name}.</li>
          <li class="flex items-center gap-2"><CheckCircle className="w-5 h-5 text-medical flex-shrink-0" /> <strong>Ponctualité garantie</strong> pour vos rendez-vous médicaux.</li>
          <li class="flex items-center gap-2"><CheckCircle className="w-5 h-5 text-medical flex-shrink-0" /> <strong>Véhicules confortables</strong> et climatisés.</li>
          <li class="flex items-center gap-2"><CheckCircle className="w-5 h-5 text-medical flex-shrink-0" /> <strong>Aide administrative</strong> pour vos dossiers CPAM.</li>
        </ul>
      </div>

      <!-- CTA Box -->
      <div class="mt-16 bg-slate-900 text-white p-8 rounded-2xl text-center shadow-xl">
        <h3 class="text-2xl font-bold mb-4">Réservez votre course à {city.name} maintenant</h3>
        <p class="mb-8 text-slate-300">Estimation immédiate et réservation sans surprise.</p>
        <a href="tel:0663039516" class="inline-block bg-white text-primary hover:bg-slate-100 font-bold py-3 px-8 rounded-full transition-colors">
          Commandez votre Taxi
        </a>
      </div>
    </div>
  </section>
</Layout>
