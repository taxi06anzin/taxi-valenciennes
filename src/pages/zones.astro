---
import Layout from '../layouts/Layout.astro';
import cities from '../data/cities.json';

const sortedCities = cities.sort((a, b) => a.name.localeCompare(b.name));
---

<Layout 
  title="Zones d'intervention Taxi Valenciennes | 40+ Communes" 
  description="Consultez la liste complète des communes desservies par Taxi Valenciennes. Transport VSL conventionné et taxi toutes distances dans le Nord (59)."
>
  <section class="bg-primary text-white py-16">
    <div class="container mx-auto px-4 text-center">
      <h1 class="text-4xl font-bold font-heading mb-4">Zones d'Intervention</h1>
      <p class="text-xl text-slate-300 max-w-2xl mx-auto">
        Nous intervenons rapidement sur Valenciennes et l'ensemble des communes de la métropole pour vos trajets médicaux (VSL) et privés.
      </p>
    </div>
  </section>

  <section class="py-16 bg-slate-50">
    <div class="container mx-auto px-4">
      <div class="bg-white p-8 rounded-2xl shadow-lg">
        <h2 class="text-2xl font-bold font-heading mb-8 text-primary border-b pb-4">Toutes nos communes desservies</h2>
        
        <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
          {sortedCities.map(city => (
            <a 
              href={`/${city.slug}`} 
              class="flex items-center p-4 rounded-lg hover:bg-slate-50 border border-slate-100 hover:border-accent transition-all group"
            >
              <span class="w-2 h-2 rounded-full bg-accent mr-3 group-hover:scale-125 transition-transform"></span>
              <span class="font-medium text-slate-700 group-hover:text-primary">{city.name}</span>
            </a>
          ))}
        </div>
      </div>

      <div class="mt-12 text-center">
        <p class="text-slate-600 mb-6">Votre commune n'est pas dans la liste ? Contactez-nous pour vérifier notre disponibilité.</p>
        <a href="tel:0698432710" class="inline-block bg-accent hover:bg-amber-600 text-white font-bold py-3 px-8 rounded-full transition-colors shadow-lg">
          📞 06 98 43 27 10
        </a>
      </div>
    </div>
  </section>
</Layout>
